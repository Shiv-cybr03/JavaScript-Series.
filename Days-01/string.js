const name = "shivkumar"
const repo = 500

console.log(name + repo);   // this is not a better 

const gameName = "gilidanta"
console.log(gameName.length);
console.log(gameName.toLocaleUpperCase());
console.log(gameName.slice(0,5));

const newName = gameName.substring(0,8)
console.log(newName);

const newString = "    shiv  "
console.log(newString);
console.log(newString.trim()); //to remove extra space on string 

const myName = "shibu"
console.log(myName.replace('b','v'));  // to replace to one word or sentence to another word or sentence 

let director = "rajamoli"
console.log(director.includes('o'));   //to chech words is present or not:

let str = "appple, banana, mango"
console.log(str.substring(0,15));  

let stName = "arjit "
console.log(stName.repeat(5));     //output is 5 times name repeat using this methods
console.log(stName.search('j'));    // to search element is present or not

let example = "ritik 'roshan' "
console.log(example);              // string declared template or methods
